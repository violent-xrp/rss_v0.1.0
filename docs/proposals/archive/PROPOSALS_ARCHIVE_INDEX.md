# Proposal Archive

Resolved proposal notes live here after implementation, rejection, or supersession.

Do not delete the design trail when a proposal is fixed. Archive it with the resolution outcome and the commit or release that closed it.

Archived proposals:

- `REPO_STRUCTURE_PROPOSAL_R1_SUPERSEDED.md` - superseded by the R1 `src/rss/` package restructure; retained as design history.
